# Introduce

This project is the source code of pNet front interface

# QuickStart

In the project directory, you can run:

```shell
npm install && npm start
```

by default this will run the node server in localhost and port 3000