# Introduce

This project is the source code of pNet backend

# QuickStart

In the project directory, you can run:

```shell
python manage.py runserver
```

by default this will run the node server in localhost and port 8000